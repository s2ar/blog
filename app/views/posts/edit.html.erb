<h1>Edit Post</h1>

<%= form_for @post do |f| %>
	<%= f.text_field :title %> <br>
	<%= f.text_area :annot %> <br>
	<%= f.text_area :description %> <br>
	<%= f.check_box :enable %> <br>
<p><%= f.submit "Сохранить"%></p>
<% end %>